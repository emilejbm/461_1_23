#!/bin/sh

#include check for num of arguments
case $1 in
    "install")
        echo "will install dependencies tbd";; 
    "build")
        echo "will compile tbd";;
    "URL_FILE") # i have no idea how to actually do this maybe regex ?
        echo "will process URLs tbd";;
    "test") # logging and testing stuff
        echo "will test and log tbd";;
esac